# Normalize line endings to LF in the repository for all text files.
* text=auto eol=lf

# Explicitly binary (never normalize).
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.webp binary
*.pdf binary
